export type SavePipelineState = {
    buttonEnabled: boolean;
    buttonLabel: string;
    buttonLoading: boolean;
};

export const savePipelineState: SavePipelineState = {
    buttonEnabled: false,
    buttonLabel: 'Approve',
    buttonLoading: false,
};

export const savePipelineState2: SavePipelineState = {
    buttonEnabled: false,
    buttonLabel: 'Withdraw',
    buttonLoading: false,
};