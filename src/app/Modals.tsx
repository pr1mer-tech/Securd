"use client";
import ModalConnection from "@/components/Modals/ModalConnection/ModalConnection";
import React from "react";

const Modals = () => {
  return <ModalConnection />;
};

export default Modals;
